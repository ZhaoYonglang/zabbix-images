name: Export Zabbix Images
on:
  workflow_dispatch:   # 手动触发
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pull and save images
        run: |
          docker pull zabbix/zabbix-server-mysql:6.4
          docker pull zabbix/zabbix-web-nginx-mysql:6.4
          docker save -o zabbix-6.4.tar zabbix/zabbix-server-mysql:6.4 zabbix/zabbix-web-nginx-mysql:6.4
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: zabbix-images
          path: zabbix-6.4.tar
